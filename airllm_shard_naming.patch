--- a/air_llm/airllm/utils.py	2026-08-22 18:04:52
+++ .venv/lib/python3.13/site-packages/airllm/utils.py	2026-08-22 21:20:12
@@ -1,6 +1,7 @@
 import gc
 import json
 import os
+import re
 import ctypes
 import shutil
 from tqdm import tqdm
@@ -307,8 +308,17 @@
     # norms) are in the list, so sort by the last shard each module touches. This is a stable sort,
     # so plain embed -> layers -> norm -> lm_head checkpoints keep their existing order.
     def _last_shard_of(layer):
-        nums = [int(v.split('-')[1]) for k, v in index.items()
-                if k.startswith(layer) and '-' in v and len(v.split('-')) > 1]
+        # Standard HF sharding names shards "model-00001-of-00015.safetensors", where
+        # v.split('-')[1] == "00001" (purely numeric). Qwen3.8-27B-FP8 instead ships one shard
+        # per layer, named "layers-0.safetensors" -- v.split('-')[1] == "0.safetensors", which
+        # int() rejects outright. Extract the leading digits instead of assuming the whole
+        # segment is numeric, so both naming schemes work.
+        nums = []
+        for k, v in index.items():
+            if k.startswith(layer) and '-' in v:
+                digits = re.match(r'\d+', v.split('-')[1])
+                if digits:
+                    nums.append(int(digits.group()))
         return max(nums) if nums else -1
 
     layers.sort(key=_last_shard_of)
@@ -376,10 +386,9 @@
     for v in set(index.values()):
         parts = v.split('-')
         if len(parts) > 1:
-            try:
-                shard_num_to_file[int(parts[1])] = v
-            except ValueError:
-                pass
+            digits = re.match(r'\d+', parts[1])
+            if digits:
+                shard_num_to_file[int(digits.group())] = v
 
     single_modelfile = None
 
@@ -406,7 +415,15 @@
 
         # Optionnally load next shard
         # checking whether after spliting from '-', if second element exists. otherwise it throws errors for single 'model.safetensor' files
-        shards = [int(v.split('-')[1]) for k, v in index.items() if k.startswith(layer) and '-' in v and len(v.split('-')) > 1]
+        # Extract leading digits rather than assuming the whole post-'-' segment is numeric --
+        # some repos (e.g. Qwen3.8-27B-FP8) name shards "layers-0.safetensors", not
+        # "model-00001-of-00015.safetensors".
+        shards = []
+        for k, v in index.items():
+            if k.startswith(layer) and '-' in v:
+                digits = re.match(r'\d+', v.split('-')[1])
+                if digits:
+                    shards.append(int(digits.group()))
         if len(shards) > 0:
             # A layer can span several shards (especially fp8 checkpoints, where each weight has a
             # companion weight_scale_inv tensor). Load *every* shard up to the highest one this layer
